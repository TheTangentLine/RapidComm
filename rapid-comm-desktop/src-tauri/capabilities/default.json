{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        { "name": "../../build/bin/rapidcomm-server", "sidecar": true }
      ]
    },
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        { "name": "../../build/bin/rapidcomm-server", "sidecar": true }
      ]
    },
    "shell:allow-open"
  ]
}
